{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build",
            "type": "shell",
            "command": "${workspaceFolder}/test/test.sh",
            "options": {
                "cwd": "${workspaceFolder}/test"
            },
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        },
        {
            "label": "Clean",
            "type": "shell",
            "command": "${workspaceFolder}/test/clean.sh",
            "options": {
                "cwd": "${workspaceFolder}/test"
            },
            "problemMatcher": []
        }
    ]
}